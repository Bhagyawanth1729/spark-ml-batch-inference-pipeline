# spark-ml-batch-inference-pipeline
End-to-end batch inference pipeline using Apache Spark MLlib for classification. Trains and saves a PipelineModel, runs batch scoring on large CSV data, and writes predictions to Parquet partitioned by date
